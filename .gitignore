# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
ai/.env
test/*
.idea/
.cursorrules
node_modules/
package-lock.json
yarn.lock
.DS_Store
package.json

# Local manual PDF downloads (spec §11) — never commit
pdfs/

# Raw crawl artifacts — site only needs data/articles/**, keep main lean
data/*.jsonl
data/rss/
/tmp/

# Per-machine module state (journal, pdf-status, last-fetch, consumer cursors)
.state/

# Local experiment outputs — keep design notes (README.md) but ignore the data
experiments/*/snapshots/
experiments/*/status.json
experiments/*/daemon.log
experiments/*/daemon.out
experiments/*/daemon.pid